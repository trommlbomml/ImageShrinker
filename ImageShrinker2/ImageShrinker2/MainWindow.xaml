<Window x:Class="ImageShrinker2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <Window.Resources>
        <DataTemplate x:Key="ImageViewDataTemplate">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="{Binding Name}" />
                <TextBlock Text="{Binding Width}" />
                <TextBlock Text="{Binding Height}" />
            </StackPanel>
        </DataTemplate>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" /> 
        </Grid.RowDefinitions>
        
        <StackPanel Orientation="Horizontal">
            <Button Content="Load" Command="{Binding AddFilesCommand}" />
            <Button Content="FromFolder" Command="{Binding AddFromFolderCommand}" />
            <Button Content="SaveToFolder" Command="{Binding SaveToFolderCommand}" />
            <Button Content="PackToFolder" Command="{Binding PackToFolderCommand}" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ListView 
                ItemsSource="{Binding Images}" 
                SelectedItem="{Binding SelectedImage}"
                ItemTemplate="{StaticResource ImageViewDataTemplate}" />
            
            <Border Grid.Column="1"  BorderBrush="Black" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBox Text="{Binding ArchiveName}" />
                    <Image Grid.Row="1" Source="{Binding SelectedImage.Path}" />
                    <Border Grid.Row="1" VerticalAlignment="Bottom" Background="Orange" Opacity="0.5">
                        <StackPanel Orientation="Vertical">
                            <Slider Width="100" Minimum="1" Maximum="100" Value="{Binding Scale}" />
                            <TextBlock Text="{Binding DesiredWidth}" />
                            <TextBlock Text="{Binding DesiredHeight}" />
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </Grid>

    </Grid>
</Window>
